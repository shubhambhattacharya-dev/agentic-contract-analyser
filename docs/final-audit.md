# Elcara — Final Audit Log

| ID | Area | Problem | Severity | Root Cause | Fix | Test |
|----|------|---------|----------|------------|-----|------|
| A01 | Ingestion | `Document could not be chunked` → upload 500 on documents containing any single token longer than 800 chars (base64 blobs, long URLs, corrupted glyphs) | P0 | `buildChildChunks` skipped the size guard for a lone word, producing an oversized child that failed `validateChunkRelationships` | Oversized tokens are now split at character boundaries into offset-true slices before child building | `chunk-hardening.test.ts` (8000-char token case + invariant assertions) |
| A02 | Data integrity | Failed ingestion left the stored raw file behind ("ghost" blob unreachable but persistent) | P1 | File stored in step 1; no cleanup on later-stage failure | Ingestion tracks the stored URL and deletes the file on any post-upload failure | code-reviewed; store.deleteFile already covered by deletion tests |
| A03 | Chat | `HybridSearchError: has no indexed chunks` surfaced as raw 500-style generic error | P1 | Chat did not distinguish "document missing" from "document index missing" | Consistency pre-check: every selected document must still have indexed chunks, else SSE `error` with actionable "no longer indexed — re-upload" message | `chat-consistency.test.ts` (ghost doc, normal doc, cross-session doc) |
| A04 | Security | Chat accepted a documentId owned by another session until retrieval failed internally | P1 | Same missing pre-check; ownership was only enforced implicitly by key lookup | Pre-check reads the chunk key under the REQUEST's session — other-session documents now fail with a controlled error before any retrieval | `chat-consistency.test.ts` case 3 |
| A05 | Dev environment | Stale `dist/index.js` processes served old binaries on port 8000 → "fixed" bugs reappearing, contradictory logs | P1 | Repeated background restarts + Windows file locks left orphan node processes | Kill-by-commandline before restart; single-process policy during dev | manual |
| A06 | Observability | Test runs flush the dev Redis (`flushdb`), wiping the developer's uploaded documents — looked like "documents vanish / no indexed chunks" | P2 | Integration tests share the dev Redis | Documented: re-upload after `npm test`; CI uses its own Redis container | n/a |
| A07 | Verification | Entity questions (phone/email/name) answered with generic refusal | P2 | Correct abstention — the test fixtures genuinely lacked those fields | Contact-rich fixture proves correct verified answers with page attribution (phone p2, email p2, parties p1) | live curl evidence + golden fixture `smoke-contract-v3-contact.pdf` |
| A08 | Chat UX | Abstain refusal lost its transparency reason (SSE-only notice) | P2 | Coverage line lived in a transient notice event | Refusal message now carries the reason text permanently | verified live |
| A09 | Frontend | First library load showed "Session is required." | P1 | `requireSession` re-parsed the (empty) request cookie header instead of trusting the middleware-assigned session | Trusts `req.sessionId` on first contact; integration test updated to the no-login contract | `document.test.ts` case 6 |
| A10 | Streaming | Stop button: partial kept but live bubble never finalized (spinner stuck) | P2 | Client abort kills SSE before `done` arrives | Client finalizes the partial locally with `status: stopped` on abort | browser E2E |
| A11 | Chat UX | Raw JSON footer visible during streaming; duplicate finalized messages under StrictMode | P2 | Prompt emitted JSON-only; state update had side effects inside an updater | Answer-first generation contract (JSON quotes footer stripped live); terminal events handled outside updaters | browser E2E |

Verified NOT bugs: `Invalid PDF structure` (fake fixture — correctly rejected 422); table/smart-quote/unicode/₹/% text (all pass with canonical-offset invariants — see hardening tests).
